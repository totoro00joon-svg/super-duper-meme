name: Update stock rankings

on:
  schedule:
    # 21:40 UTC = 06:40 KST (개장 전), 08:10 UTC = 17:10 KST (장 마감 후)
    - cron: "40 21 * * 0-4"
    - cron: "10 8 * * 1-5"
  workflow_dispatch: {}

permissions:
  contents: write

# 같은 워크플로우가 겹쳐 돌면 커밋이 충돌하므로 한 번에 하나만 실행
concurrency:
  group: update-rankings
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Run ranking script
        run: python scripts/rank.py

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/data.json docs/history.json docs/track.json
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "chore: update stock rankings ($(date -u +'%Y-%m-%d %H:%M UTC'))"
            git push
          fi
